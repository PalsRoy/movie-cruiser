import React from 'react';
import './styles.css';

const AddFailure = ({stylingProperty}) => (
<div className={stylingProperty}>
Failed to add the movie details to Favourite!
</div>
);

export default AddFailure;
